# 플릿 하드닝 — Tasks

> 권위: [spec.md](./spec.md) / [README](./README.md). `[x]`=완료, `[ ]`=잔여, `[~]`=진행 중.
> **`[server]` = 사람이 라이브에 적용(§11).** 표시 없는 항목은 에이전트가 레포에 산출물을 **생성**하는 것까지다.
> 크기: **S**=반나절 이내 · **M**=1~3일 · **L**=1주+.
> 라이브 파일 직접 편집 금지(§12) — 레포에서 고치고 사람이 복사한다.

## 진행 현황

| 축 | 내용 | 태스크 | 완료 | AC |
|---|---|---|---|---|
| 1 | GPU 스택 정합성 + node-hygiene 커버리지 구멍 | 14 | **8** | 17 |
| 2 | 물리 디스크 SMART 가시화 (RAID 뒤) | 20 | **13** | 15 |
| 3 | GPU 장애 런북 + runbook_url 무결성 게이트 | 11 | **9** | 14 |
| 4 | 섀시 전력 배선 + 드리프트 규칙 검증 게이트 | 12 | **7** | 19 |
| 5 | CI 파이프라인 (§9 이행) | 26 | **21** | 20 |
| **합계** | | **83** | **58** | **85** |

> 완료는 **레포 산출물 기준**이다. `[server]` 태스크(라이브 배포·정비창)는 사람이 적용해야 끝난다(§11).
> 축1·축3의 미완분은 대부분 그 범주다 — T1-4(배포)·T1-13(정비창)·T3-10(프로비저닝 복사) 등.

## 권장 파동

| 파동 | 태스크 | 왜 이 순서 |
|---|---|---|
| **W0** | T5-1 ~ T5-5 · **T5-26** | 다른 축의 게이트가 얹힐 바닥. 지금 red인 것을 먼저 green으로. 도구가 없으면 실행기가 rc=2다 |
| **W1** | **T1-12** → T1-1 ~ T1-11 (**T1-13은 병행, T1-14는 T1-13 이후 언제든**) | **되돌릴 수 없는 순서 제약**(README §4.1). promtool 해석기와 **폴백 엔진**이 먼저 있어야 AC-1-10이 돈다(**T5-26 전·새 클론 기준** promtool도 docker 접근권도 없어 폴백 없이는 exit 2다 — spec §0.2.2. W0의 T5-26이 promtool을 `~/.local/bin`에 설치하므로 W1 시점의 이 호스트는 `--which=path`다). T1-13(재부팅 티켓)은 정비창 협의라 파동에 묶이지 않고, T1-14(알림 승격)는 부채가 0이 된 뒤에만 |
| **W2** | T4-1 ~ T4-12 · T3-1 ~ T3-11 | 라이브 변경이 SIGHUP·프로비저닝 리로드뿐. **CI 도입 시점 red 8종 중 5종이 여기서 해소된다**(spec §7.2 표) |
| **W3** | T5-6 ~ T5-25 | 앞 축의 게이트가 다 떨어진 뒤 켜야 첫날 red를 피한다 |
| **W4** | T2-1 ~ T2-20 | 유일하게 신규 role이 생기고 연구 노드에서 주기 작업이 새로 도는 축 |

> [!CAUTION]
> **파동을 기다리지 않는 티켓 2건 — 지금 열 수 있다.**
> - **T2-18(열화 디스크 2본 처분)**: data04 `ZC1AE78X`(GDL 773)·`ZC1968JB`(GDL 66 + 미교정 9)는 지금 열화 중이고, 축2는 가시화까지이지 교체가 아니다.
> - **T1-13(재부팅 부채 3건 청산)**: 부채 나이 실측 **.103 = 16.0일**(0→1 전이 2026-07-17 06:24 UTC) · **.104 = ≥30일**(TSDB 보존 30d 전 구간이 1이라 상한을 알 수 없다). 이 부채가 0이 되기 전에는 T1-14(알림 승격)를 하지 않는다 — 보존 30d가 표현 가능한 임계의 상한인데 그 상한(`min[30d]`)에서도 .104가 발화하므로 **임계 조정으로는 day-1 발화를 못 없앤다**(spec §1.2 D1-4 선택지 표).

> [!CAUTION]
> **data05만 `sudo -n`이 실패한다 — 여러 축의 `[server]` 태스크에 걸린다.** [실측 2026-08-02]
> `sudo -n true` data05 rc=**1** / data01·data03·data04 rc=**0**. `ansible.cfg`가 `become = True`라 **Ansible 드라이런도 data05에서 실패**한다(실측 `sudo: a password is required`). data05에서 `docker`는 sudo 없이 못 쓰고(`docker` 그룹 아님) `/data/monitoring/`은 root 소유다.
> **영향 태스크 전수와 T0-6 전 대안 경로(`-K` / 대화형 `sudo`)는 [README](./README.md) §4.2.1이 정본이다** — T1-4·T1-7·T1-9 · T2-15·T2-16 · T3-10 · T4-8·T4-9·T4-10이 해당한다. T5-26은 apt 경로를 택할 때만.
> **sudoers 교정 자체는 라이브 변경이라 사람 작업(hardware-ops T0-6 `[server]`)이다**(§11·§12). 에이전트가 고치지 않는다.

---

## 축 1 — GPU 스택 정합성 탐지 + node-hygiene 커버리지 구멍 교정

> [!WARNING]
> **T1-11(증거 캡처)이 hardware-ops T0-4(data05 재부팅)의 하드 게이트다.** 재부팅이 먼저 일어나면 `node_nvidia_version_mismatch` 1→0 전이가 시계열에 남지 않고 **되돌릴 수 없다.** 위반 여부는 AC-1-13이 `query_range`로 **재부팅 후 30일까지** 판정하고(TSDB 보존 30d), 그 뒤로는 T1-11이 커밋한 JSON 스냅샷이 유일한 증거다.

> [!NOTE]
> **축1의 data05 특권 처리**(전수·근거는 위 CAUTION과 README §4.2.1) — T1-4는 **드라이런 ①까지 포함해** hardware-ops T0-6 선행 또는 `-K`가 필요하다(`become=True`라 `--check`도 특권을 요구한다). data05 로컬 셸에서 도는 T1-7·T1-9는 **대화형 `sudo`**로 수행한다. data01·data03·data04는 NOPASSWD라 `-K` 없이 그대로 돈다 [실측].

- [x] **T1-12** (M) `scripts/gates/promtool.sh` + `tools/promtool_fallback.py` **(둘 다 헬퍼 — `check-`로 시작하지 않거나 `gates/` 밖이라 글롭 대상 아님, §0.2)** — ⓐ **해석기**: ① PATH의 `promtool` ② `docker run --rm --entrypoint=/bin/promtool -v "$PWD:/w" -w /w prom/prometheus:v3.11.3 "$@"` ③ 캐시된 Release 바이너리 ④ `KEIWI_PROMTOOL_ALLOW_DOWNLOAD=1`일 때만 다운로드(sha256 검증). `--which`는 `path|docker|cache|none`을 1줄 출력하고 **항상 rc=0**, 실제 호출은 전부 실패 시 rc=2 + 시도 경로를 stderr에. **`KEIWI_PROMTOOL_ENGINE=none`이면 해석을 전부 건너뛰고 `none`으로 동작한다**(폴백 강제 스위치 — T5-26이 promtool을 설치한 뒤에도 AC-1-16 ②③이 폴백 경로를 검증할 유일한 수단, spec §0.2.1). **`docker run … prom/prometheus … promtool …`(`--entrypoint` 없이)은 ENTRYPOINT가 `/bin/prometheus`라 동작하지 않는다** — 이 태스크의 존재 이유다. ⓑ **폴백 엔진**(§0.2.2): `check-rules`(YAML 구조·필수키·record 이름 정규식·`expr` 괄호/따옴표 균형)와 `check-metrics`(노출 형식 lint) 두 서브명령. 순수 Python + PyYAML 6.0.1(실측 존재)만 쓴다. **`test rules` 폴백은 만들지 않는다** — PromQL 평가 엔진이 필요하고, 흉내내면 거짓 초록이 된다. **ⓐ와 ⓑ를 같은 태스크로 묶는 이유**: 해석기만 만들면 promtool 없는 환경에서 여전히 전부 exit 2다. 이 호스트 실측(**T5-26 전 시점**): promtool 없음 · `docker` **그룹 미가입**으로 소켓 permission denied(sudoers 문제가 아니라 T0-6으로도 안 풀림) · github.com은 도달 가능(200/206). T5-26(W0) 후에는 `--which=path`가 정상이고(`~/.local/bin`이 PATH에 있다 [실측]) 폴백은 새 클론·CI 이전 환경의 몫이다. **레포 전역 자산**(축4 T4-4·축5 T5-15·축2 T2-10이 재사용). **선행: 없음.** 검증: AC-1-16
- [x] **T1-1** (M) `roles/node-hygiene/tasks/main.yml` 가드 분할 — `_nodeexp_default.stat.exists`를 `set_fact: node_hygiene_is_apt_node`로 좁혀 **ARGS lineinfile 태스크(30행)에만** 적용하고, textfile 디렉터리·마커·스크립트·`.service`·`.timer`·timer enable **6개의 가드 제거**(가드가 걸린 태스크는 총 **7개**). ⚠️ **14-19행 "apt 없으면 안내 후 스킵" debug 태스크도 삭제**(남기면 data01·data05에서 SKIPPED가 잡혀 이 스펙이 없애려는 동작을 문서가 계속 주장한다) + **1-6행 헤더 주석 교체**("data05는 자동 스킵"이 거짓이 된다). ⚠️ **같은 거짓 주장이 `playbooks/agents.yml`에도 있다 — 38-40행 주석("apt 노드만 실제 적용 · data05는 role이 자동 스킵")과 41행 play 이름 끝의 "apt 노드". 이 태스크에서 함께 교체한다**(role만 고치면 플레이북이 옛 동작을 계속 주장한다). 같은 파일 **9행의 `-K` 안내("예 data04")도 교체** — 실측상 data04는 NOPASSWD이고 `-K`가 필요한 노드는 **data05**다(README §4.2.1). 상단에 `node_hygiene_consumer` assert + host 노드 소비처 실증 검증(`pgrep`으로 `--collector.textfile.directory` 확인) 추가 — **pgrep 태스크에 `check_mode: false` 필수**(shell/command 모듈은 `--check`에서 스킵돼 `rc` 부재 → `rc | default(1) != 0`이 참이 되어 data01(consumer=host + 非apt)이 드라이런에서 **거짓 실패**하고 AC-1-2가 착수 시점부터 red다. `changed_when: false`·`failed_when: false`와 함께 3종 세트로 — **레포의 기존 관용구다**: `roles/gpu-model-exporter/tasks/main.yml:9-13`·`roles/port-exporter/tasks/main.yml:7-11`, 그리고 같은 role 8행 stat 태스크의 12행). **선행: 없음.** 검증: AC-1-1·AC-1-2·AC-1-14
- [x] **T1-2** (S) `roles/node-hygiene/defaults/main.yml`에 `node_hygiene_consumer: host` · `node_hygiene_apt_enabled: true` · `node_hygiene_nvidia_enabled: true` 추가. `inventory.ini`에 data05 `node_hygiene_consumer=container`, data01 `node_hygiene_apt_enabled=false`(16.04 EOL 248건 영구대기 = 신호 아님). **선행: T1-1.** 검증: AC-1-9
- [x] **T1-3** (M) `templates/keiwi-node-hygiene.sh.j2`에 NVIDIA 정합성 블록 추가 — **hardware-ops T0-1을 구현하는 태스크**(메트릭 이름 4개 그대로). 교정 2건 필수: (a) 유저스페이스를 `ldconfig -p`→`readlink -f`로 해석(**하드코딩 `/usr/lib/x86_64-linux-gnu` 금지** — data01은 `/usr/lib/nvidia-418`), (b) `node_nvidia_probe_ok`·`node_nvidia_smi_exit_code` 추가로 판정불능/미수집 구분. `nvidia-smi -L`은 `timeout 20` + `|| rc=$?`. `/proc/driver/nvidia/version` 부재 시 nvidia 메트릭 **전부 미노출**. apt 블록을 `{% if node_hygiene_apt_enabled %}`로 감쌈. **선행: T1-2.** 검증: AC-1-5·AC-1-6·AC-1-7
- [ ] **T1-4** `[server]` (M) node-hygiene 배포 — ① 드라이런 `ansible-playbook -i inventory.ini playbooks/agents.yml --tags node-hygiene --check --diff -K`(4노드 `failed=0`·`unreachable=0`) — **`-K`가 드라이런에서부터 필요하다**(`ansible.cfg` `become=True` + data05 `sudo -n` rc=1. 실측: 없이 돌리면 data05가 `sudo: a password is required`로 `failed=1`). T0-6 완료 후에는 생략 가능 ② 저위험 순서: **data03(기존 노드, 회귀 확인) → data04 → data01 → data05**(data01을 data05보다 앞: §7.3, 16.04 EOL 노드에서 문제 시 중단 가능하게). **파동 내 순서일 뿐 data05를 다음 파동으로 미루는 것이 아니다**(README §4.1) ③ **`-K`가 불필요한 것은 data01·data03·data04다**(4노드 전수 실측 `sudo -n true` rc=0) — **data05만** rc=**1**(`(ALL) NOPASSWD: ALL` 뒤에 `(ALL : ALL) ALL`이 와서 마지막 규칙이 이긴다). data05 구간은 hardware-ops **T0-6 선행**(권장, README §4.2.1 경로 A) 또는 **`-K`로 진행**(경로 B)하며, 되돌릴 수 없는 순서 제약 때문에 **T0-6이 안 끝났으면 기다리지 않고 `-K`**. 프롬프트를 1회로 줄이려면 `--limit data05 -K`로 분리 실행한다. 비번을 `-e ansible_become_password=`로 넘기지 않는다(§13) ④ 각 노드 `systemctl is-active keiwi-node-hygiene.timer` = active. **선행: T1-3.** 검증: AC-1-3·AC-1-4·AC-1-8
- [x] **T1-5** (S) `rules/keiwi-hygiene-coverage.yml` 신규 — **record 전용 10건**: 커버리지 6건(`fleet:node_exporter_up:count`·`fleet:node_hygiene_reporting:count`·`fleet:node_hygiene_coverage:gap`·`fleet:gpu_driver_mismatch:count`·`fleet:gpu_driver_probe_failed:count`·`instance:node_uptime:days`) + **재부팅 부채 4건**(`fleet:node_reboot_required:count`·`instance:node_reboot_debt:min7d`·`:min14d`·`:min30d`). **alert 키 금지**(기존 `rules/` 파일 헤더 규약). **선행: T1-3, T1-12.** 검증: AC-1-10
- [x] **T1-6** (M) 알림 **2건**을 `grafana/provisioning/alerting/alert-rules.yaml`에 추가 — `NodeHygieneCoverageGap`(gap>0, 30m) · `NodeHygieneStale`(신선도 5400s, 15m). 런북 2개 신규: `docs/runbooks/node-hygiene-coverage-gap.md`·`node-hygiene-stale.md`(frontmatter는 축3 D3-2 계약, `kind: alert`). ⚠️ **`RebootRequiredStale`은 여기서 만들지 않는다** — 실측상 `min_over_time(node_reboot_required[14d])`가 .103·.104 **둘 다 1**이라 적용 즉시 상주 발화 2~3건이고, 그것이 T0-7이 지목한 "알림 무시 습관의 시작"이다. **임계를 올려도 못 피한다**: 보존 30d가 표현 가능한 창의 상한인데 `count(min[30d] == 1)`이 **1**(.104)이다. 승격은 T1-14(부채 청산 후). **`NvidiaVersionMismatch`·`NvidiaSmiFailing`은 hardware-ops spec.md:555-566이 이미 정의 — 재정의 금지.** **선행: T1-5.** 검증: AC-1-11·AC-1-15
- [ ] **T1-7** `[server]` (S) 규칙 라이브 적용 — `rules/keiwi-hygiene-coverage.yml`을 `/data/monitoring/rules/`로 **복사**(§12) → `bash scripts/gates/check-rules.sh --check infra/monitoring/rules/keiwi-hygiene-coverage.yml`(**`engine=promtool` 확인** — 라이브 적용 직전이라 축소 강도로 통과한 것을 근거로 삼지 않는다. `promtool.sh`를 직접 부르지 말 것: 엔진이 없으면 exit 2다) → `docker kill -s HUP prometheus` → Grafana 프로비저닝 재적용. 발화 폭탄 방지를 위해 **T1-4 완료 후 30분 뒤**(gap이 0으로 수렴한 뒤). ⚠️ data05 `sudo -n` 실패 — **대화형 `sudo`로 수행**(스크립트로 감싸지 말 것). 직후 **T4-12**(스냅샷 재생성)를 잊지 않는다. **선행: T1-4, T1-6.** 검증: AC-1-3·AC-1-11·AC-1-15
- [x] **T1-8** (S) `infra/monitoring/docker-compose.yml` — node-exporter의 `/run/dbus/system_bus_socket` 마운트에서 **`:ro` 제거**(소켓 `connect()`는 쓰기 권한 필요). `/run/systemd:ro`는 유지. 주석에 실측 근거(`node_scrape_collector_success{collector="systemd",instance=".105"}`=0, `node_systemd_unit_state` .105 시리즈 0개) 기재. **T1-9 진단 결과가 다르면 이 태스크를 재설계한다.** **선행: 없음**
- [ ] **T1-9** `[server]` (S) data05 systemd 수집기 진단 후 복구 — ① `docker logs node-exporter 2>&1 | grep -i 'systemd\|dbus'`로 **원인 확정**(추정만으로 적용 금지) ② 가설 일치 시 T1-8 적용 후 `docker compose up -d node-exporter`(해당 서비스만, ~15초 공백, 관제 호스트라 연구 워크로드 무관) ③ 불일치 시 로그를 T1-8에 회신하고 재설계. ⚠️ 두 단계 다 **대화형 `sudo`**가 필요하다 — data05에서 `docker ps`가 `permission denied … docker.sock`이고(`mooner92`는 `docker` 그룹 아님) `sudo -n`은 rc=1이다 [실측]. **선행: T1-8.** 검증: AC-1-12
- [x] **T1-10** (S) 교차 참조 갱신(드리프트 방지) — `specs/hardware-ops/tasks.md` T0-1에 "구현은 fleet-hardening T1-3", T0-2에 "**선행: T1-4** — 현행 role은 data05를 stat 가드로 스킵하므로 이 태스크 단독으로는 기대치(data05 mismatch=1) 달성 불가", T0-4에 "**선행: T1-11**(증거 보존)", **T0-6에 "막고 있는 것"에 축1 T1-4 추가**(원문에 이미 "축1의 data05 배포 전체"라 적혀 있으므로 fleet-hardening 쪽 태스크 id만 명시). `spec.md:690`의 `readlink libnvidia-ml.so.1` 구현안에 data01 경로 예외 실측 각주 추가. **선행: 없음**
- [ ] **T1-11** `[server]` (S) **증거 캡처 — hardware-ops T0-4(data05 재부팅) 이전에 반드시 수행.** mismatch=1 상태를 최소 1시간 이상 관측한 뒤 ① `query_range` 응답을 **JSON 파일로 커밋**(`docs/runbooks/evidence/nvidia-mismatch-data05.json`) ② Grafana 패널 스크린샷 1장을 `docs/runbooks/nvidia-driver-mismatch.md`에 첨부. **JSON 커밋이 핵심이다** — TSDB 보존이 정확히 30d라 AC-1-13은 재부팅 후 30일까지만 판정할 수 있고, 그 뒤 영구 증거는 이 파일뿐이다. **이 태스크 완료가 T0-4의 게이트다.** **선행: T1-4**
- [ ] **T1-13** `[server]` (S) **재부팅 부채 청산 티켓 — W1을 기다리지 않는다.** 대상 3건: data03(**부채 16.0일** — 0→1 전이 2026-07-17 06:24 UTC 실측, uptime 28.79d) · data04(**부채 ≥30일 — 보존 한계라 상한 미상**, `min[30d]`도 1, uptime **151.30d**, pending linux-image 8개 → **먼저 처리**) · data05(T1-4 후 메트릭 도달, 재부팅은 hardware-ops **T0-4** 소관이므로 여기서는 추적만). 정비창 협의 → 연구 잡 대피 안내(§11: 자동 조치 금지) → 재부팅. **T2-18(열화 디스크 티켓)과 같은 성격 — 축1은 측정·노출까지이고 실행은 티켓이다.** **선행: 없음**
- [ ] **T1-14** `[server]` (S) **`RebootRequiredStale` 승격** — `min_over_time(node_reboot_required[14d]) == 1`, for 15m, sev3, 런북 `docs/runbooks/reboot-required-stale.md` 신규. **적용 전 관문 2개**: ① `q 'fleet:node_reboot_required:count'` = **0** ② `q 'count(min_over_time(node_reboot_required[14d]) == 1) or vector(0)'` = **0**(현재 **2**). 둘 중 하나라도 0이 아니면 그만큼이 즉시 발화 대상이라는 뜻이므로 **승격을 멈춘다.** ⚠️ **②를 "빈 벡터"로 쓰지 않는다** — 수집기가 `node_reboot_required 0`을 항상 방출하므로(`keiwi-node-hygiene.sh.j2` 31·53행) 재부팅 뒤에도 시리즈가 살아 있어 `min_over_time`은 **0을 반환**하고 빈 벡터가 되는 경우가 없다. 관문식을 알림식과 동일하게 두면 "관문 통과 = day-1 발화 0"이 항등식이 된다. 임계 14일은 실측이 아니라 **정책값**(정비창 확보 기간)임을 런북에 명시 — 실측 분포는 이 값을 **고를 수 없다**는 것만 알려준다(spec §1.2 D1-4). **선행: T1-13, T1-7.** 검증: **AC-1-17**(관문 2개 재확인 + 규칙·런북 존재 + 적용 후 발화 0 — 이 태스크 완료 후에는 AC-1-15 ③의 "규칙 부재" 판정이 AC-1-17로 대체된다, 2단 판정)

---

## 축 2 — 물리 디스크 SMART 가시화 (RAID 컨트롤러 뒤)

- [x] **T2-1** (M) `infra/ansible/roles/disk-smart-textfile/templates/keiwi-disk-smart.sh.j2` — 컨트롤러 sg 탐색(scsi `type==12`), `-d cciss,N` 열거(조기 종료·어댑터 스킵·`timeout`), **python3 `json.load` 파싱**(정규식·jq 금지 — jq는 노드에 없다), spec §2.2 D2-3 메트릭만 방출, 원자적 `.prom` 교체(node-hygiene 패턴). **선행: 없음.** 검증: AC-2-6·AC-2-12·AC-2-13
- [x] **T2-2** (M) role 나머지 — `defaults/main.yml`(textfile_dir · `max_index=24` · `miss_streak=4` · `probe_timeout=15` · `interval=15min` · `smartctl_path` · `min_version=7.0`) / `tasks/main.yml`(가드: textfile dir 존재 **+** `smartctl>=7.0`, 없으면 debug 스킵. ⚠️ **버전 조회 태스크에 `check_mode: false` 필수** — `command`/`shell`은 `--check`에서 스킵돼 register에 `rc`가 없고 가드가 통째로 거짓이 된다. 그러면 T2-12 드라이런이 "changed 3개" 대신 전부 skipped를 내고 AC-2-10은 **공허하게** 통과한다. node-hygiene pgrep 태스크와 같은 결함이다 — spec §1.2 D1-1의 `--check` 규약) / `handlers/main.yml` / `.service`·`.timer` 템플릿(`Nice=10`·`IOSchedulingClass=idle`·`ReadWritePaths`·`NoNewPrivileges`, **`PrivateDevices` 금지**). **선행: T2-1.** 검증: AC-2-10
- [x] **T2-3** (S) `playbooks/agents.yml`에 play 추가 — `tags: [disk-smart]`, `hosts: nodes`, role `disk-smart-textfile`. **기존 `smartctl` play는 건드리지 않는다.** **선행: T2-2**
- [x] **T2-4** (S) `infra/monitoring/disk-smart/README.md` — 왜 기성 `smartctl_exporter`가 아닌가(B1 라벨 충돌 `smartctl.go:45` / B2 SCSI miner 미실행 `smartctl.go:116`, 파일·행 인용), **`cciss,N`의 N은 베이가 아님**, 킬 스위치, data01 vendoring 분기, 업스트림 기여 여지(백로그). **선행: 없음**
- [x] **T2-5** (S) `rules/keiwi-recording.yml`에 record 4건 추가(spec §2.2 D2-5). ⚠️ **이 파일은 `record:` 전용 — alert 키 금지**(파일 헤더 규약). **선행: 없음**
- [x] **T2-6** (M) `dashboards/syshealth.json` 개편 — row `id=200` 제목을 「디스크 건강 (물리 디스크 · 논리 볼륨)」으로, 물리 디스크 stat/table/topk/증가추세 4패널 **추가**, 패널 1·5 제목에 "논리 볼륨" 명시, gridPos 재계산. ⚠️ **죽은 패널 6·7·8 제거는 이 태스크가 하지 않는다 — 축4 T4-6(W2)으로 이관했다.** 세 패널이 참조하는 메트릭이 918개 스냅샷에 없어 축4 가드가 W2에 켜지는 순간 CI가 W4까지 red가 되기 때문(spec §0.3). **선행: T2-5, T4-6.** 검증: AC-2-9
- [x] **T2-7** (S) `infra/monitoring/prometheus.yml` 정리 — data04 `:9634` 터널 활성화 안내 블록(**119-127행**) **삭제**, "물리 디스크는 node-exporter textfile 경로로 수집한다(specs/fleet-hardening §2)" 주석으로 대체. ⚠️ **128행(공백)·129-131행은 남긴다** — 129-131은 GlitchTip job의 `ENABLE_OBSERVABILITY_API=True` 경고이고 이 축과 무관하다(지시대로 131까지 지우면 그 운영 경고가 사라진다). **선행: 없음.** 검증: AC-2-8
- [x] **T2-8** (M) `alert-rules.yaml` — 신규 3건(`DiskGrownDefectsGrowing`·`DiskUncorrectedErrorsGrowing`·`PhysicalDiskDisappeared`, **전부 increase 기반**, `noDataState=NoData`) 추가 + `keiwi-smart-fail` refId A의 expr을 `smartctl_device_smart_status or node_smart_disk_health_passed`로 교체(**PromQL 합집합** — 별도 refId + 조건 OR 는 라벨 매칭 0건이라 Grafana 에서 성립하지 않고 `classic_conditions` 는 라벨을 접는다. spec §2.2 D2-6 경고 상자). **승격은 `specs/alerting`의 2주 섀도 절차를 따른다 — 이 태스크는 규칙 생성까지.** ⚠️ **적용 순서 강제**: 세 규칙은 `noDataState=NoData`라 `node_smart_*`가 라이브에 없는 상태에서 반영하면 **첫날부터 `DatasourceNoData` 3건이 상주 발화**한다. T2-13·T2-14(수집기 배포) 후 `q 'count(node_smart_disks)' != 0`을 확인하고 T2-16에서 반영한다(위생 커버리지 2건이 T1-4 뒤에 켜지는 것과 같은 규약). **선행: T2-6.** 검증: AC-2-11·AC-2-14
- [x] **T2-9** (S) `docs/runbooks/disk-grown-defects.md` — 킬 스위치 1줄 → 증상 → 판별(`sudo smartctl -a -d cciss,N /dev/sgX`) → **serial로 물리 특정** → HPE 예비 부품/교체 절차 → 오탐 판정(단발 vs 지속 증가). frontmatter는 축3 D3-2 계약을 따른다. **축3 `smart-health-failed.md`의 §한계 절 갱신도 이 태스크 소관**(수집 구조가 바뀌므로). **선행: T2-8.** 검증: AC-2-15
- [x] **T2-10** (S) 게이트 1종 + 헬퍼 1종 — `scripts/gates/check-smart-metric-allowlist.sh`(**게이트**: 스크립트 방출 메트릭 ↔ spec 승인 목록 대조, `--render-check` 플래그로 픽스처 렌더 + 노출 형식 검사 — `promtool.sh --which`가 엔진을 주면 `check metrics`, `none`이면 `tools/promtool_fallback.py check-metrics`로 내려가고 **어느 쪽이든 `EXPOSITION_OK engine=…` + rc=0**이어야 한다. `SKIP` 금지)와 `scripts/gates/render-smart-fixture.sh`(**헬퍼**: 고정 JSON 픽스처 → `.prom`). 픽스처 4케이스: data03 SAS 1본 · data04 SATA SSD 1본 · 어댑터 응답 · 부재 응답. ⚠️ **`render-smart-fixture.sh`는 `check-`로 시작하지 않아 `verify-all.sh` 글롭에 안 잡힌다**(§0.2) — 반드시 `check-smart-metric-allowlist.sh`가 호출하도록 배선한다. **선행: T2-1, T1-12.** 검증: AC-2-6·AC-2-12
- [x] **T2-11** (S) data01 분기 설계 — `infra/monitoring/disk-smart/smartmontools-xenial/README.md`(정적 7.x 바이너리 vendoring, `infra/logging/filebeat-xenial` 전례 인용) + role의 `disk_smart_smartctl_path` 오버라이드 경로. **hpsa에서 `-d cciss,N` 동작은 미검증이라 배포는 T2-17 뒤로 미룬다.** **선행: T2-2**
- [x] **T2-12** `[server]` (S) data03 드라이런 **[2026-08-03 실행 — `changed=3`(스크립트·유닛·타이머) 확인, 변경 0. data01은 `changed=0`으로 사유 debug 출력]** — `ansible-playbook playbooks/agents.yml --tags disk-smart --check --limit data03`. changed 항목이 예상(스크립트·유닛·타이머 3개)과 일치하는지 확인. **선행: T2-3**
- [ ] **T2-13** `[server]` (M) data03 실적용 → `systemctl start keiwi-disk-smart.service` → `.prom` 확인 → `q 'node_smart_disks{instance="192.168.1.103:9100"}'` = **12**. **선행: T2-12.** 검증: AC-2-1·AC-2-4·AC-2-5·AC-2-7
- [ ] **T2-14** `[server]` (M) data04 적용 — 신규 포트·터널 변경 없음을 배포 전후 `ss -ltn`으로 대조. 기대: `node_smart_disk_grown_defect_list{serial="ZC1AE78X"}`=**773**, `{serial="ZC1968JB"}`=**66**, `node_smart_disk_uncorrected_errors_total{serial="ZC1968JB",op="read"}`=**8**. **이 값이 그래프에 뜨는 순간이 이 축의 성공 증명이다.** **선행: T2-13.** 검증: AC-2-2·AC-2-8
- [ ] **T2-15** `[server]` (S) data05 물리 디스크 실측 — **선행: T2-14. hardware-ops T0-6(sudoers 순서 교정)은 권장이지 차단이 아니다** — 미해소 시 실측은 **대화형 `sudo`**, 뒤이은 role 적용은 **`--limit data05 -K`**로 진행한다(README §4.2.1). `smartctl -i -d cciss,N /dev/sg4`(N=0..24)로 대수·모델·시리얼 확보 후 spec §2.1의 "미측정" 칸을 실측으로 채운다. 그다음 role 적용
- [ ] **T2-16** `[server]` (S) 대시보드 import + 규칙 리로드 + **SMART 3규칙 noDataState OK→NoData 복원**(2026-08-04 E1 배포가 적용 순서를 어겨 임시 OK로 강등됨 — alert-rules.yaml 주석 참조) — `syshealth.json` 재import(**`docker cp` 금지**, 바인드 경로 복사), recording rule을 `/data/monitoring/rules/`에 복사 후 `docker kill -s HUP prometheus`. ⚠️ 셋 다 data05에서 **대화형 `sudo`**(README §4.2.1 — `/data/monitoring`은 root 소유, `docker`는 그룹 미가입). **선행: T2-14.** 검증: AC-2-9
- [ ] **T2-17** `[server]` (S) data01 사전 검증 — 벤더링한 정적 smartctl 7.x를 임시로 놓고 `-d cciss,0 /dev/sg0`(P840ar/hpsa)가 물리 디스크를 반환하는지 확인. **반환하지 않으면 data01을 이 축 범위 밖으로 명시하고 spec에 사유를 남긴다**(추측으로 배포하지 않는다 — 27.3T LV이 사각지대로 남는 사실을 숨기지 않는다). **선행: T2-11**
- [ ] **T2-18** `[server]` (S) **열화 디스크 2본 처분 티켓** — data04 `ZC1AE78X`(GDL 773, POH 63,777h)·`ZC1968JB`(GDL 66, 미교정 read 8/write 1). HPE 예비 4TB SAS 재고 확인 + 어레이 이중화 수준 확인 후 교체 계획. **이 축은 가시화까지이고 교체는 별건이다 — 알림으로 만들지 않고 티켓으로 남긴다.** **선행: 없음(W4를 기다리지 않는다)**
- [ ] **T2-19** `[server]` (S) 2주 섀도 후 알림 승격 판단 — 신규 3건의 발화 횟수 집계. `specs/alerting` 승격 조건 충족 시에만 Ticket/Page 배정. 동시에 **alerting spec §10-3의 열린 질문**(`SmartHealthFailed` noDataState 승격) 종결. **선행: T2-16**
- [x] **T2-20** (S) `docs/decisions/0024-physical-disk-smart-collection.md` — 헌장 §8 이행. 담을 것: ① 업스트림 `smartctl_exporter` 기각 근거(**B1 `smartctl.go:45`**·**B2 `smartctl.go:116`** 소스·행 인용, v0.14.0 및 master 동일) ② 신규 role `disk-smart-textfile`을 만드는 이유(node-hygiene 확장 기각 근거 포함) ③ 신규 이름공간 `node_smart_*`(LV 사실과 물리 사실을 섞지 않는다) ④ data01 정적 바이너리 vendoring(`infra/logging/filebeat-xenial` 전례) ⑤ **되돌리기 조건 — 업스트림이 B2(`interface_ == "scsi"` 게이트)를 고치면 이 결정을 재검토한다**(FB-01과 연결). ADR 번호는 0023(축5) 다음. **선행: T2-4**

---

## 축 3 — GPU 장애 런북 + runbook_url 무결성 CI 게이트

- [x] **T3-1** (M) `docs/runbooks/gpu-xid.md` 작성 — §1 latched 게이지의 의미(값 아닌 `changes`) · §2 30초 판별(Prometheus 코드 + OpenSearch 원문 대조, **`service=unknown`·`node` 라벨 부재 함정 명시**) · §3 Xid 코드 분기표(43/13/31=앱, 48/63/64/74/79=HW) · §4 확증 신호의 한계 실측(remap은 A40만, ECC 없음, DCGM은 mismatch 못 봄) · §5 소유자 확인(`gpu_model_info` user) · §6 조치 트리 · §7 사후. frontmatter `alerts: [GpuXidErrorNew, GpuXidCritical]`. **선행: 없음**
- [x] **T3-2** (S) `docs/runbooks/gpu-thermal.md` — 92°C 임계 근거와 **현재 여유 2°C**(30일 최대 90, data04 GPU1) · 스로틀 대체 판별(`SM_CLOCK` 급락 + `GPU_UTIL` 유지) · **쓰면 안 되는 신호**(`MEMORY_TEMP`) · `nvidia-smi -q -d TEMPERATURE`는 미측정이므로 명령만 제공하고 숫자를 단정하지 않는다 · `nvidia-smi -pl`은 연구 성능 직접 영향이라 사람 판단(§11). **선행: 없음**
- [x] **T3-3** (M) 나머지 4종 작성(공통 5절 골격) — `node-down.md`(exporter down vs 노드 down 분기, **data04 터널 오판 경로**) · `disk-pressure.md`(DiskUsageHigh+DiskFillPredicted) · `memory-pressure.md`(MemoryLow+OomKillOccurred) · `smart-health-failed.md`(**RAID 뒤 물리디스크 미노출 한계를 먼저 명시**, 수집 구조는 축2 링크만). 최소 골격 30초 템플릿도 함께 산출. **선행: 없음**
- [x] **T3-4** (S) **기존 런북 3종 정비 — 게이트 day-1 red 3건을 미리 없앤다**(spec D3-9). 실측 red 목록:
  - `log-ingestion-stopped.md`: ① frontmatter **전무**(1행이 `# 런북 · …`) → 추가(`id`/`kind: alert`/`alerts: [LogIngestStalled]`/`category`/`severity`/`signature`/`last_verified`). 콘솔 어시스턴트 인덱싱 누락도 이것으로 해소. ② **본문은 66행 한 줄만** — `ssh -p 764 <user>@<ip> '…'` → `ssh -p 764 "<user>@<ip>" '…'`. **R10은 지금 초록이다**(뒤따르는 인용문이 우연히 `>`의 대상이 되어 문법만 통과한다) → **day-1 red가 아니라 예방**이고, 복붙 시 `user: No such file or directory`로 죽는 것은 실측으로 확인했다. 표기를 통일해 두지 않으면 다음 편집에서 rsyslog와 같은 red가 난다.
  - `rsyslog-omfile-flood.md`: ① `kind: incident` **한 줄 추가**(없으면 기본값 `alert`로 간주돼 R6b가 `alerts`·`severity`를 요구한다) ② **41행 `ssh -p 764 <user>@<node-ip>` → `ssh -p 764 "<user>@<node-ip>"`** — bash가 `<`를 입력 리다이렉션으로 파싱해 **R10(`bash -n`)이 실패한다**(실측 `SYNTAX FAIL: …:40`, **`:40`은 ` ```bash ` 블록 시작 행**이고 명령은 41행이다). 따옴표만 씌우면 통과하고 사람이 읽는 의미는 그대로. **이것이 런북 3파일·bash 블록 11개 전수 중 유일한 R10 red다**(실측) — T5-6로 `verify-all.sh`를 켜기 전에 반드시 없앤다.
  - `node-onboarding.md`: **변경 0.** 이미 `id`·`kind: procedure`·`category`를 갖고 있다 — `kind` 분류(D3-2)를 도입한 덕분에 거짓 `alerts: []`·`severity`를 달지 않아도 된다. `<…>` 자리표시자 4곳은 **주석 안(147행)·작은따옴표 안(148행)**이라 R10에 걸리지 않는다(실측).
  - **표기 규약**: `<…>` 자리표시자는 따옴표 안에 둔다. T3-1~T3-3이 만드는 런북 6종과 최소 골격 템플릿에도 적용한다(spec D3-9).
  
  **선행: 없음.** 검증: AC-3-6·AC-3-8
- [x] **T3-5** (M) `scripts/gates/check-runbooks.sh` 작성 — R1~R11(R6/R6b는 `kind` 2층 계약, R10은 **블록 단위** `bash -n` + 실패 블록 시작 행 출력), exit 0/1/2(§0.2), `--check-main`·`--self-test`·`--quiet`. Grafana provisioning YAML과 (생기면) `infra/monitoring/alerts/*.yml` 양쪽 파싱. **알림 개수를 하드코딩하지 않는다** — 축1 T1-6이 2건, 축2 T2-8이 3건을 더한다. `--self-test` 위반 픽스처는 **런타임 생성**(`mktemp -d`), 커밋 금지(D5-1과 동일 원칙). **hardware-ops T2-5를 인수한다.** **선행: 없음**
- [x] **T3-6** (S) `alert-rules.yaml` 교정 — 9건 runbook_url 재배선(spec §3.2 D3-1 표) + `:204` summary **92°C** + `:17`·`:173` 주석 최대값 88→**90**/여유 4→**2** + `:249` XID 43 원인 주석을 원문 근거(`pid`/`name=VLLM::Worker`)에 맞게 교정. **축5 T5-11의 P4 게이트가 이 교정으로 red→green이 된다.** **선행: T3-1, T3-2, T3-3, T3-4.** 검증: AC-3-3·AC-3-5
- [x] **T3-7** (S) 배선·인덱스 — `docs/README.md` 런북 표에 신규 6종 + '담당 알림' 열 추가 · `apps/console/src/lib/runbooks.test.ts` 회귀 테스트 추가(`loadRunbooks().length == docs/runbooks/*.md` 개수). **`apps/console/package.json`에는 추가하지 않는다** — 레포 전역 게이트는 `scripts/verify-all.sh`가 정본 실행기(§0.2). **선행: T3-5.** 검증: AC-3-7·AC-3-11
- [x] **T3-8** (S) 기존 스펙 정합 — `specs/hardware-ops/tasks.md` T2-5를 "**fleet-hardening T3-5로 이관**(대상 경로 `scripts/gates/check-runbooks.sh`)" 표기 · `spec.md:427` kebab 규칙을 **frontmatter `kind`+`alerts:` 선언 방식**으로 갱신하고 **`spec.md:645` AC-2-6의 경로도 같은 "이관" 표기**로 정정(`scripts/check-runbooks.sh` → `scripts/gates/check-runbooks.sh`). ⚠️ **두 곳 다 "이관" 단어를 남긴다** — AC-5-19가 실재하지 않는 게이트 참조를 `미구현\|이관`으로 판정하므로, 표기를 빼면 그 AC가 red로 남는다 · **T4-3(`tasks.md:78`)의 세 항목 삭제 제안** — `gpu-xid-critical.md`→`gpu-xid.md`, **`node-down.md`·`exporter-down.md`→축3 `node-down.md` 하나**(exporter down vs 노드 down 분기를 §2에 담아 흡수한다. 파일을 나누면 두 문서가 서로를 가리키다 둘 다 낡는다). 나머지 3종(`psu-redundancy-lost`·`inlet-temp-near-critical`·`sel-near-full`)은 **BMC 축 소관 — 건드리지 않는다** · T0-3에 frontmatter 계약 요구 한 줄 추가. **선행: T3-5**
- [x] **T3-9** (S) CI 축 핸드오프 문서 — **워크플로 파일은 만들지 않고**(축5 소관), 잡에 넣을 스텝 스니펫(`pip install pyyaml` → `bash scripts/gates/check-runbooks.sh` → post-merge `--check-main`)과 exit 2 처리 규약만 spec에 남긴다. **선행: T3-5**
- [ ] **T3-10** `[server]` (S) `alert-rules.yaml`을 `/data/monitoring/grafana/provisioning/alerting/`로 **복사** 후 Grafana 프로비저닝 리로드(§11·§12). **사전 조건: 발화 중인 알림 0건일 때 수행**(리로드가 `for` 타이머를 초기화한다). ⚠️ data05에서 **대화형 `sudo`**(README §4.2.1). **선행: T3-6.** 검증: AC-3-12·AC-3-13
- [ ] **T3-11** `[server]` (S) 테스트 발화 1건(예: DiskUsageHigh 임시 임계 하향 또는 Grafana 규칙 preview)으로 Slack 본문의 '런북:' 링크를 **실제로 눌러 200 확인** 후 원복. **선행: T3-10.** 검증: AC-3-14

---

## 축 4 — 섀시 전력 배선 + 드리프트 recording rule 검증 게이트

> [!NOTE]
> **hardware-ops T1-1~T1-5와 같은 파일명을 쓴다.** 파일을 분기하면 두 정본이 생기고 사람이 어느 쪽을 복사할지 판단해야 하는 순간 §12 사고가 난다. 이 축은 그 파일들의 **교정된 내용**을 공급한다.

- [x] **T4-1** (M) `rules/keiwi-hardware.yml` 생성 — `keiwi_power`(**10** 레코드 — 초안의 "9"는 계수 오류다. spec §D4-1이 나열하는 것은 chassis·reporting_count·watts_sum·gpu_power·gpu_watts_sum·fleet share·instance share·nongpu·chassis kWh·gpu kWh **10개**) + `keiwi_firmware_drift`(3 레코드). **메트릭명 `node_hwmon_power_average_watt`(단수)** 확인(`_watts`는 라이브에 없다). kWh는 **원 메트릭 기반**(레코딩 시리즈 참조 금지 — 적용 후 24h 과소집계). `record:`만. **선행: 없음.** 검증: AC-4-1·AC-4-2
- [x] **T4-2** (S) `rules/keiwi-standards.yml` 생성 — 드라이버 카운트에 `{DCGM_FI_DRIVER_VERSION!=""}` 필터 + **`fleet:gpu_driver_unlabeled:count` 동반 필수**. `count_hygiene`는 `or vector(0)` **없이**(0을 "드리프트 없음"으로 오독하게 된다). **선행: 없음.** 검증: AC-4-1·AC-4-2
- [x] **T4-3** (M) `rules/tests/keiwi-hardware.test.yml` + `keiwi-standards.test.yml` — **구 규칙에서 반드시 실패하는 케이스를 반드시 포함**(U30/2.2 vs U30/2.4, 라벨 부재 GPU 2장). 이 테스트가 있었다면 두 거짓 규칙은 머지되지 않았다. **선행: T4-1, T4-2.** 검증: AC-4-3·AC-4-4
- [x] **T4-4** (M) `scripts/gates/check-rules.sh` + `scripts/gates/check-promql-metrics.sh` + `tools/promql_metric_guard.py` — **promtool 호출은 전부 `scripts/gates/promtool.sh`(T1-12) 경유**(raw `docker run … prom/prometheus … promtool`은 ENTRYPOINT 때문에 동작하지 않는다). `check-rules.sh`는 서브명령 `--check`/`--test`/`--test --schema-only`를 갖는다(AC-4-1·4-3·4-4가 각각 호출). **엔진 분기는 게이트 안에 있다**(§0.2.2): `--check`는 promtool이 없으면 `tools/promtool_fallback.py check-rules`로 내려가 **rc가 절대 2가 되지 않고**, `--test`는 폴백이 없어 `SKIP(env: promtool)` + rc=2다. 첫 줄에 `RULES_OK engine=promtool|structural`을 반드시 찍는다 — 축소 강도를 감추면 "안 돌았는데 초록"이다. 가드는 §0.3의 두 스냅샷 파일 **합집합**을 허용집합으로 쓰고 pending 항목에 근거 주석이 없으면 exit 1. ⚠️ **토크나이저 요구사항** — 코퍼스 실전 결과 오탐 2건(`Reallocated_Sector_Ct`=라벨 **값**, `apt_upgrades_pending`=부분 토큰)이 나왔다. 문자열 리터럴 안과 라벨 매처 키를 제외하는 상태기계가 필요하고 이 두 케이스를 `--self-test` 픽스처로 못 박는다. **이 상태기계는 `tools/promtool_fallback.py`가 노출하는 것을 import해 쓴다**(T1-12) — 두 벌 만들면 한쪽만 고쳐져 갈라진다. **Makefile은 만들지 않는다** — `scripts/verify-all.sh` 글롭이 자동 편입(§0.2). **선행: T4-3, T1-12.** 검증: AC-4-5·AC-4-16
- [x] **T4-5** (S) `infra/monitoring/metric-names.txt` 스냅샷 커밋(읽기 전용 조회, **918개 중 `:` 포함 119개는 recording rule 이름**) + `infra/monitoring/metric-names.pending.txt`(축1 `node_nvidia_*` 6종 · 축2 `node_smart_*` 전량, 각 줄에 `# spec §N` 근거 필수). 갱신 절차를 각 파일 헤더 주석에 기록하고 **"배포 후 T4-12로 재생성"을 명시**. **선행: 없음.** 검증: AC-4-6 ⚠️ **축1 T1-3 산출 6종을 반드시 포함**: `node_nvidia_kernel_module_version`·`_userspace_version`·`_version_mismatch`·`_probe_ok`·`_smi_ok`·`_smi_exit_code`(축1은 정본 소유자가 이 태스크라 등록하지 않았다 — 여기서 빠지면 어디에도 안 남는다).
- [x] **T4-6** (M) `dashboards/syshealth.json` — ① row 「전력」(패널 6개) + row 「표준 드리프트」(패널 **7개**: BIOS 드리프트·BIOS 인벤토리·드라이버 버전 수·**드라이버 미관측 GPU**·커널 릴리스 수·**재부팅 부채 ≥14일 노드**(`count(instance:node_reboot_debt:min14d == 1) or vector(0)`)·**재부팅 부채 창 table**) 추가 — gridPos **y=24·y=37**(spec §D4-5 정정본 — 초안의 y=32는 폐기), uid·기존 패널 id 불변, 신규 row id 400/500대. ⚠️ **「재부팅 대기 노드」라는 제목의 stat을 새로 만들지 않는다** — 같은 제목·같은 의미의 패널 id 2가 row 100에 **이미 있다**(실측). 신규 stat은 **T1-14 승격 임계와 같은 양**(창 14d 부채 노드 수)이라 의미가 겹치지 않는다. **플릿 전력 stat에 `reporting_count` 보조 타깃 필수** ② ⚠️ **row 200의 죽은 패널 6·7·8 삭제**(원래 축2 T2-6/W4였으나 이관) — `smartctl_device_percentage_used`·`_attribute`·`_available_spare` 세 이름이 918개 스냅샷에 **없어서** W4까지 미루면 축4 가드·축5 CI가 두 파동 내내 red다(spec §0.3·§7.2). 그 자리는 T2-6이 물리 디스크 패널로 채운다. `noValue: "0"`·`${datasource}` 기존 관용구 준수. **선행: T4-1, T4-2.** 검증: AC-4-15·AC-4-16·**AC-4-19**
- [x] **T4-7** (S) `specs/hardware-ops/{spec.md,tasks.md}` 거짓 서술 **11곳** 교정(spec §4.2 D4-6 표) — `product:node_bios_versions:count`·`instance:node_bios_age:days` 제거(B10 종결), AC-3-1 기대값 2→**1**(+unlabeled=4), AC-3-4를 `count_hygiene=1 AND unlabeled=0`으로 교체, `spec.md:261` 'BIOS 드리프트 현재 2' 삭제, `spec.md:676` 주석의 `535.309.01` 제거, kWh 식 교체(**`> 0` 필터 포함** — 없으면 data01이 0kWh로 뜬다), T1-1/T1-2 레코드 목록 갱신, **`tasks.md:44`(T1-3)의 `docker compose restart prometheus` → `docker kill -s HUP prometheus`**(같은 파일명을 쓰는 이상 필수 — 이 스펙 §7.3이 2026-07-02 대시보드 소실 사고의 원인으로 지목하고 T4-9가 금지한 명령이다), **`spec.md:551`의 `BiosVersionDrift` 알림 — 레코드명 교체가 아니라 존폐 판단까지**: 참조 레코드가 삭제되므로 그대로 두면 영구 no-data 죽은 알림이다. **존치하되 `expr: max(fleet:node_bios_drift:count) > 0`로 재작성**(+`:554` 주석 "현재 2"→"현재 0"). 근거: 드리프트 탐지 의도는 hardware-ops 알림 축 소관으로 유효, 교정 레코드가 같은 의도 표현, 현재값 0이라 day-1 발화 없음 — 활성화 판단은 hardware-ops에 남긴다, **`tasks.md:151`(B10 백로그)에 "fleet-hardening T4-7에서 삭제로 종결" 표기**(README §3.1) — 이 두 곳(D4-6 10·11행)까지 지워야 AC-4-13이 통과한다(교정 전 실측: hardware-ops 히트 8줄 중 초안 9곳은 6줄만 덮었다). **선행: T4-1, T4-2.** 검증: AC-4-13·AC-4-14
- [ ] **T4-8** `[server]` (S) 레포 클론에서 `bash scripts/gates/check-rules.sh && bash scripts/gates/check-promql-metrics.sh` 통과 확인 후 — ⚠️ **여기서는 `engine=promtool`을 확인하고 진행한다.** 라이브에 규칙을 넣기 직전이므로 `engine=structural`(§0.2.2 축소 강도)로 통과한 것을 근거로 삼지 않는다. T5-26이 W0에서 promtool을 깔아두므로 정상 순서를 지켰다면 자동으로 충족된다. 그리고 `keiwi-hardware.yml`·`keiwi-standards.yml`을 `/data/monitoring/rules/`에 **복사**(라이브 편집 금지 §12), `root:root 644`. ⚠️ data05는 `sudo -n` 실패 — **대화형 `sudo`로 수행**(자동화 스크립트로 감싸면 조용히 실패한다). **선행: T4-4, T4-7**
- [ ] **T4-9** `[server]` (S) `sudo docker kill -s HUP prometheus`로 설정 재적용. **`docker compose restart` 금지**(스크레이프·평가 공백 + 컨테이너 재생성 리스크). `--web.enable-lifecycle=false`라 HTTP reload(405)는 불가. 직후 `prometheus_config_last_reload_successful`=1 **그리고** 신규 그룹 `health=ok`를 **둘 다** 확인. **선행: T4-8.** 검증: AC-4-7~AC-4-12·AC-4-17·AC-4-18
- [ ] **T4-10** `[server]` (S) `syshealth.json`을 Grafana 프로비저닝 바인드 경로에 복사 후 반영 확인. **`docker cp` 주입 금지**(`infra/monitoring/README.md:135-136` CAUTION — 2026-07-02 익명뷰어 적용 재생성 때 keiwi-gpu·model-workload·logs 대시보드 소실 사고. 같은 취지가 `:46`·`docs/runbooks/node-onboarding.md:119`에도 있다). uid `keiwi-syshealth` 유지 확인. ⚠️ 복사는 data05에서 **대화형 `sudo`**(README §4.2.1). **선행: T4-6, T4-9**
- [ ] **T4-11** (S) `fleet:gpu_driver_versions:count_hygiene`가 값을 내는지 확인하고, 값이 나오면 syshealth 「표준 드리프트」 row의 **정본 지표를 DCGM 기반에서 hygiene 기반으로 교체**(DCGM 지표는 부분집합으로 강등). **선행: 축1 T1-4**(hardware-ops T0-2가 아니다 — 구현 주체가 축1이다) ⚠️ **2026-08-03 확인: 아직 불가.** `q 'count(count by (version)(node_nvidia_kernel_module_version))'` → **빈 벡터**(축1 T1-4 미배포 = 생산자 부재). 규칙은 `or vector(0)` 없이 들어가 있어 무해하게 비어 있고, T4-6이 넣은 패널 502(「GPU 드라이버 버전 수 (DCGM 관측)」) description에 **"부분집합 지표이며 정본은 count_hygiene, 교체는 T4-11"**을 이미 명시해 두었다 — 이 태스크가 남은 일은 패널 교체뿐이다.
- [ ] **T4-12** `[server]` (S) **배포 직후 메트릭명 스냅샷 재생성·커밋** — Prometheus는 recording rule 이름을 `__name__`으로 노출하므로(918개 중 **119개가 `:` 포함** [실측]) 새 record를 배포하는 순간 커밋된 `metric-names.txt`가 낡는다. **T1-7·T2-16·T4-9 각각의 직후**에 `curl -sG localhost:9090/api/v1/label/__name__/values | jq -r '.data[]' | sort > infra/monitoring/metric-names.txt`로 재생성하고, 라이브에 나타난 이름을 `metric-names.pending.txt`에서 **제거**한다(AC-4-6 ②가 검사). **선행: T4-9**(및 T1-7·T2-16 각 배포 직후 반복). 검증: AC-4-6

---

## 축 5 — CI 파이프라인 (헌장 §9 이행)

### 5.A 선결과제 — 지금 red인 게이트를 green으로

- [x] **T5-26** `[server]` (S) **게이트 도구 설치 — W0 선행.** 실측상 data05에 `yamllint`·`shellcheck`·`ansible-lint`·`promtool`이 **전부 없고** docker 소켓은 permission denied다. 이 상태에서는 `verify-all.sh`가 rc=0이 될 수 없다(rc=2가 정상). 설치: `pipx install yamllint ansible-lint`(시스템 파이썬 오염 최소화, `pipx` 없으면 `python3 -m venv ~/.local/keiwi-gates`) + `shellcheck`는 **정적 바이너리를 기본**으로 한다(`~/.local/bin`) — apt 경로는 data05에서 `sudo` 비번을 요구하고(README §4.2.1), W0을 T0-6에 묶을 이유가 없다. **promtool도 설치한다** — Release tarball(`prometheus-3.11.3.linux-amd64`)에서 `promtool`만 꺼내 `~/.local/bin`에 두고 sha256을 릴리스의 `sha256sums.txt`와 대조한다. 이유: `check-rules.sh --test`(PromQL 평가 엔진 필요)와 `check-prometheus.sh`에는 **폴백이 원리적으로 없다**(§0.2.2). 설치 전에는 verify-all 요약표에 `check-prometheus.sh`의 `SKIP(env)` 1건이 남고(글롭의 인자 없는 `check-rules.sh`는 SKIP 대신 `--test --schema-only` 자동 강등 `NOTE` — spec §0.2·D4-4), 명시적 `check-rules.sh --test`(AC-4-3·4-4 로컬 판정)는 별도로 `SKIP(env: promtool)` rc=2다. 설치해야 이 태스크의 목표인 `SKIP(env) 0건`(+ 자동 강등 `NOTE` 0건 = 전강도)이 성립한다. `docker` 그룹 가입은 요구하지 않는다 — 로컬 `check-prometheus.sh`는 promtool만 있으면 `WARN: 글롭 동형성 미검증` + rc=0으로 올라서고, 동형 마운트 판정은 CI가 정본이다. **T1-12의 폴백 엔진은 이 설치를 대체하지 않고 보완한다** — 설치 전(그리고 다른 사람의 새 클론)에도 `--check` 계열이 돌게 하는 것이 폴백의 몫이다. 설치 후 `verify-all.sh` 요약표에 `SKIP(env)` 0건 확인. 이 호스트는 **관제 호스트**이므로 관제 스택 컨테이너·서비스에 손대지 않는 사용자 레벨 설치만 한다(§12). **선행: 없음.** 검증: AC-5-2 **[2026-08-03 실측] 완료 — `yamllint 1.38.0`·`shellcheck 0.10.0`·`ansible-lint 26.6.0`·`promtool 3.11.3` 전부 `~/.local/bin`에 설치돼 있고 `promtool.sh --which`가 `path`를 반환한다. `verify-all.sh` 요약표 `SKIP(env)` 0건 확인.**
- [x] **T5-1** (M) `apps/console/scripts/check-no-secrets.sh` 재작성 — S1(자격증명, 레포 전체·**예외 0**) / S2(배포결합 리터럴, 런타임 소스만 + `next.config.ts`) / S3(번들 노출, **fail-loud**) / S4(`.env` 추적, 레포 전체) 4규칙 분리. **허용리스트 0줄.** ⚠️ **정규식 엔진은 `python3` `re`로 못 박는다** — S1 패턴이 `(?i)`·`\s`·`{10,}` PCRE 문법이라 `grep -E`에서는 **조용히 안 잡힌다**(`(?i)`가 리터럴 취급, `\s` 미매칭). bash 래퍼 + python3 히어독. **선행: 없음.** 검증: AC-5-3·AC-5-4·AC-5-6·AC-5-7
- [x] **T5-2** (S) 진짜 결함 ① — `src/config/env.ts`에 `getSiteUrl()` 신설(`NEXT_PUBLIC_SITE_URL`, 미설정 시 `http://localhost:3105`), `src/app/layout.tsx:9,24`의 실도메인 교체, `.env.example`에 키·설명 추가. **선행: 없음.** 검증: AC-5-3
- [~] **T5-3** `[server]` (S) 진짜 결함 ② — `next.config.ts:31` `allowedDevOrigins`를 `KEIWI_DEV_ORIGINS` env 파생으로(기본값 `["127.0.0.1","localhost"]`, 배포값 0). `.env.example`에 사고 이력 주석 이관. **완료 조건: 격리 worktree에서 `node_modules/.bin/next dev -p 3199`로 띄워 클릭 반응 직접 확인 후 종료**(하이드레이션 회귀는 정적 검사로 안 잡힌다). ⚠️ **`npm run dev`를 쓰지 말 것** — 스크립트가 `next dev -p 3105`이고 **:3105는 라이브 콘솔이 점유 중**(`next-server` pid 22388, 실측)이라 §12 위험이다. 포트가 스크립트에 박혀 있어 플래그로 못 덮는다. 절차는 `docs/testing.md`의 격리 worktree 규약을 따른다. **선행: 없음.** 검증: AC-5-6 **[2026-08-03] 코드 완료 — env 파생(`KEIWI_DEV_ORIGINS`)으로 교체하고 `.env.example`에 사고 이력 이관까지 끝냈다. 남은 것은 `[server]` 확인 1건뿐이다: 격리 worktree에서 `node_modules/.bin/next dev -p 3199`로 띄워 클릭 반응을 눈으로 보는 것(하이드레이션 회귀는 정적 검사로 안 잡힌다). 에이전트가 하지 않은 이유는 §12 — 이 검증은 dev 서버를 띄우는 행위이고 포트 오설정 시 라이브(:3105)와 충돌한다.**
- [x] **T5-4** (S) 문서 예시·픽스처 정리 — ① `src/lib/grafana-host.ts:45` JSDoc과 `grafana-host.test.ts:4` 픽스처의 실도메인을 RFC 2606 `grafana.example.com`으로 ② **`scripts/sentry-payload-probe.mjs:51`의 `SECRET_TOKEN` 통짜 리터럴을 런타임 조립으로** — `["xoxb","PROBE","SECRET","TOKEN"].join("-")`. **S1 8패턴을 `git ls-files` 전수에 돌린 결과 유일한 히트**(`slack_token`·`generic` 두 패턴에 동시 매칭)이고, 값을 바꾸면 `sentry-scrub.ts:34`의 `/\bxox[baprs]-[A-Za-z0-9-]{6,}/` 검증이 무의미해지므로 **런타임 값은 유지하고 소스 리터럴만 없앤다**. 테스트는 순수 문자열 비교라 동작 불변(`npm run test` 88건 유지 확인). ⚠️ **이 태스크·spec·README에 그 리터럴을 옮겨 적지 않는다** — S1 코퍼스가 `git ls-files` 전체라 스펙 문서가 커밋되는 순간 그것도 히트가 되어 AC-5-3이 red로 남는다(spec §5.2 CAUTION 규약, 실측 3행). **선행: 없음.** 검증: AC-5-3
- [x] **T5-5** (S) `--self-test` — 각 규칙이 자기 패턴을 탐지하는지 역검증(S1은 **8패턴 개별**). 못 잡으면 rc=1. ⚠️ **픽스처를 파일로 커밋하지 않는다** — 이 레포는 PUBLIC이고 GitHub push protection은 유효성과 무관하게 패턴만 보고 차단하므로 **게이트를 도입하는 PR 자체가 push 불가**가 된다. 실행 시 접두사를 분할 결합해(`"ghp"+"_"+"A"*36` 등) `mktemp -d`에 쓰고 검사 후 삭제하며, 커밋되는 것은 **생성 로직뿐**이다. 픽스처 경로가 필요하면 **`apps/console/scripts/fixtures/`**(콘솔 스코프 — 콘솔 스크립트가 레포 루트 `scripts/gates/fixtures/`를 읽으면 §0.2 경계가 무너진다). 이 판단을 **ADR-0023에 근거로 기록**(T5-21). **선행: T5-1.** 검증: AC-5-5

### 5.B 게이트 레지스트리

- [x] **T5-6** (S) `scripts/gates/lib.sh`(**헬퍼** — gate_begin/gate_fail/요약표) + `scripts/verify-all.sh` — ① `scripts/gates/check-*.{sh,py}` **글롭 순회 — 각 게이트를 인자 없이 부른다**(따라서 `check-rules.sh`는 D4-4 인자 없음 규약을 탄다: promtool 부재 시 `--test`→`--test --schema-only` 자동 강등·`NOTE`·rc 절대 2 아님 → **verify-all의 `SKIP(env)` 출처는 `check-prometheus.sh` 하나다**, spec §0.2) ② `apps/console/scripts/check-no-secrets.sh`·`check-no-raw-hex.sh`를 **각 1회** 호출. ⚠️ **`scripts/gates/check-secrets.sh` 래퍼를 만들지 않는다** — 만들면 글롭과 ②가 같은 검사를 두 번 돌린다(AC-5-20). 헬퍼(`promtool.sh`·`lib.sh`·`render-*`)는 `check-`로 시작하지 않아 글롭에 안 잡히며, 반드시 어떤 게이트가 호출해야 한다. **기본 build 제외**, `--with-build`·`--dry-run` 플래그. `$PWD`가 `/KEIwi`이면 `--with-build` 거부 가드. **요약표에 게이트별 `PASS/FAIL/SKIP(env)`을 찍고 SKIP이 1건이라도 있으면 전체 rc=2**(§0.2 — "안 돌았는데 초록"을 만들지 않는다). **선행: 없음.** 검증: AC-5-2·AC-5-16·AC-5-20
- [x] **T5-7** (S) `.yamllint.yml`(**`key-duplicates: error`**, `line-length=disable`, `truthy=warning`, comments 완화) + `scripts/gates/check-yaml.sh` — `git ls-files '*.yml' '*.yaml'` 38개 대상. ⚠️ **규칙 이름은 `key-duplicates`다** — `duplicate-key`라는 규칙은 yamllint에 존재하지 않고, 그렇게 쓰면 `invalid config: no such rule`로 게이트가 **exit 2**가 된다(출력 메시지 `duplication of key`는 `key-duplicates`가 낸다). **선행: T5-26.** 검증: AC-5-13
- [x] **T5-8** (S) `scripts/gates/check-json.sh` — 대시보드 전수 파싱 + 최상위 `uid` 유일성 + `title` 존재. **도입 시점 red 1건을 이 태스크가 직접 해소한다**(spec §7.2 표 9번): `logs.json`·`logs.import.json`이 **둘 다 `uid: keiwi-logs`**(10파일 중 unique 9 [실측]). **정본은 `logs.json`** — 라이브 프로비저닝 디렉터리(`/data/monitoring/grafana/provisioning/dashboards/keiwi/`)에 실재하는 것은 `logs.json`뿐이고 `keiwi-dashboards.yaml` 헤더 주석도 `logs.json(uid=keiwi-logs)`을 지목한다 [실측]. `logs.import.json`은 `__inputs`/`__requires` 키를 가진 **UI 수동 임포트용 옛 사본**(version 1·패널 2 vs 정본 version 3·패널 5)으로, 프로비저닝 도입 후 용도가 소멸했다 → **삭제**(개명은 uid 중복을 해소하지 못한다). 삭제 근거를 커밋 메시지에 남긴다. ⚠️ `keiwi-*` vs `keiwi-*-v3` **정본 결정은 여전히 hardware-ops T2-4 소유** — 그 계열은 uid가 서로 달라 이 게이트의 중복 판정 대상이 아니며, 여기서는 정하지 않는다. **선행: 없음.** 검증: AC-5-14
- [x] **T5-9** (S) `scripts/gates/check-shell.sh` — 추적된 `*.sh` 전수에 `shellcheck -S warning`(node_modules는 git 미추적이라 자동 제외, 대상 4개). 위반 시 **억제 주석 남발 금지 — 스크립트를 고친다.** **선행: T5-26**
- [x] **T5-10** (S) `scripts/gates/check-python.sh` — `gpu-model-exporter`·`port-exporter` `py_compile` + `python3 -X dev` import 스모크. 두 exporter는 py3.6 폴백 대상이므로 문법 상한을 주석으로 명시. **선행: 없음**
- [x] **T5-11** (M) `scripts/gates/check-grafana-provisioning.py` — P1 데이터소스 uid 유일성 · P2 name 유일성 · P3 알림 uid/title 유일성 · P4 `evaluator.params[0]` ↔ `summary` 수치 정합(summary에 숫자 0개면 skip). `--check <rule>`로 개별 실행. **선행: 없음.** 검증: AC-5-11·AC-5-12
- [~] **T5-12** (S) P5 참조 무결성 규칙 추가 — `datasourceUid ∈ (프로비저닝 uid ∪ {__expr__})`. 현재 `bflbhyfj7rzlsb`가 위반. **hardware-ops T2-1 완료 후에 워크플로에 배선**한다(지금 강제하면 축5가 축2를 블로킹해 CI가 red인 채 도입된다). **선행: T5-11 + hardware-ops T2-1** **[2026-08-03] 규칙 구현 완료 — `check-grafana-provisioning.py`의 `ref-integrity`로 들어 있고 `--check ref-integrity`로 실행된다(현재 10건 위반: 알림이 참조하는 Prometheus 데이터소스가 레포에 프로비저닝돼 있지 않다). **기본 실행과 CI에는 의도적으로 넣지 않았다** — 지금 켜면 축5가 축2를 블로킹해 CI가 도입 첫날부터 red다. hardware-ops T2-1 완료 후 `RULES` 표에서 이 규칙의 기본 플래그를 `True`로 바꾸는 한 줄이 배선의 전부다.**
- [x] **T5-13** (S) `datasources/elasticsearch.yaml` **삭제** — 라이브에 없는 파일이고 `opensearch.yaml`과 name·uid(`keiwi-logs-es`)가 중복이라 §12의 복사 절차가 그대로 uid 충돌이 된다. 삭제 근거를 `opensearch.yaml` 헤더 주석에 남긴다. **선행: 없음.** 검증: AC-5-11
- [x] **T5-14** (S) `[축3 조율]` `keiwi-gpu-temp-high` summary 교정은 **축3 T3-6 소관 — 여기서 재정의하지 않는다.** T5-11의 P4 게이트가 red→green으로 전환되는 것만 확인한다(게이트가 실제로 결함을 잡았다는 증거). **선행: T5-11, T3-6.** 검증: AC-5-12
- [x] **T5-15** (M) `scripts/gates/check-prometheus.sh` — **라이브 동형 마운트**(`/etc/prometheus/prometheus.yml`, `/etc/prometheus/rules`)로 `check config`. **`--entrypoint=/bin/promtool` 필수**(생략하면 이미지 기본 ENTRYPOINT `/bin/prometheus`가 실행돼 즉시 죽는다). 이미지 `prom/prometheus:v3.11.3`(라이브 실측) 핀, `USER nobody`로 도니 마운트 파일 others-readable 확인. docker pull 3회 재시도 → 실패 시 `promtool.sh`(T1-12)가 찾은 로컬/캐시 바이너리로 내려가되 **마운트 동형성이 사라지므로 `WARN: 글롭 동형성 미검증`을 찍고 exit 0**. **docker도 promtool도 없으면 `SKIP(env: docker)` + exit 2** — `check config`에는 §0.2.2 폴백을 만들지 않는다(prometheus 설정 스키마 전체를 순수 Python으로 재현하는 것은 두 번째 파서를 만드는 일이고, 그 파서가 틀리면 거짓 초록이 된다). 이 호스트 기본 상태가 바로 그 rc=2이고, T5-26(promtool 설치) 후 WARN 경로로 올라선다. 축4 `check-rules.sh`(단위 테스트·alert 키 금지)와 역할이 겹치지 않는다. **선행: T1-12.** 검증: AC-5-8
- [x] **T5-16** (M) `scripts/gates/check-ansible.sh` + `scripts/gates/render-templates.py`(**헬퍼** — `check-ansible.sh`가 호출, 글롭 대상 아님) — `ansible-lint`(profile `moderate`) + `--syntax-check` 2개 + j2 7개 오프라인 렌더(`ChainableUndefined`, **문법 오류만** 실패) → 렌더된 `.sh`는 shellcheck, `.yml`은 yamllint. **molecule은 도입하지 않는다**(ADR-0023에 근거). **선행: T5-7, T5-9, T5-26.** 검증: AC-5-9·AC-5-10
- [x] **T5-17** (S) `scripts/gates/check-compose.sh` — `docker compose -f <monitoring|logging|error-tracking>/docker-compose.yml config -q`. 미설정 변수 경고는 통과, 문법·스키마 오류만 실패. **선행: 없음**

### 5.C 워크플로와 배선

- [x] **T5-18** (M) `.github/workflows/ci.yml` — 3잡(`console`·`repo-gates`·`infra-iac`), `concurrency`(cancel-in-progress), `permissions: contents: read`, **paths 필터 없음**, **서드파티 액션 0**. console 잡은 `npm ci → lint → typecheck → test → build → check:secrets → check:no-raw-hex` 순서 고정(**build가 반드시 S3보다 앞**). 액션 major는 `gh api repos/actions/checkout/releases/latest --jq .tag_name` 등으로 **실제 확인 후 핀**(추측 금지). **`infra-iac` 잡에 promtool 설치 스텝을 넣는다**(서드파티 액션 0을 지키려 마켓플레이스 액션이 아니라 `run:` 한 스텝으로 — curl + `sha256sum -c` + `install -m 0755`, 버전 `3.11.3` 핀, sha256은 릴리스 `sha256sums.txt`로 **실제 확인 후 리터럴 박기**·추측 금지). 이 스텝이 있어야 로컬에서 `SKIP(env)`이던 `check-rules.sh --test`가 **CI에서 실제로 판정된다**(AC-4-3·AC-4-4의 정본). **선행: T5-1~T5-17.** 검증: AC-5-1·AC-5-15
- [x] **T5-19** (S) `apps/console/package.json`에 `"engines": {"node": ">=22.0.0"}` + 레포 루트 `.nvmrc`(22) — 로컬(v22.22.2 실측)과 CI setup-node 버전을 한 곳에서 고정. **선행: 없음**
- [x] **T5-20** (S) `.github/workflows/release.yml` — `on: push: tags: ['v*']` → `verify-all.sh --with-build` + `CHANGELOG.md`에 해당 태그 섹션 존재 검증. **v0.2.0이 게이트 red 상태로 태그된 사건에 대한 직접 대응.** **선행: T5-6.** 검증: AC-5-17
- [x] **T5-21** (S) `docs/decisions/0023-ci-pipeline.md` — GitHub 호스티드 전용/self-hosted 기각 · **molecule 기각 근거 4가지** · 서드파티 액션 0 · 러너 시크릿 0 · §I-1(온프렘 only)과의 관계(레포는 이미 PUBLIC, CI는 메트릭·로그·도메인 데이터를 다루지 않음) · **Playwright 시각 QA는 CI 비대상**(살아있는 콘솔+Grafana 필요, `docs/testing.md` 격리 빌드가 담당) · 오탐은 허용리스트가 아니라 규칙 재정의로 없앤다는 원칙 · **self-test 픽스처를 커밋하지 않고 런타임 조립하는 이유**(PUBLIC 레포 push protection이 유효성과 무관하게 패턴으로 차단 → 게이트 도입 PR 자체가 막힌다. 다음 사람이 "왜 픽스처 파일이 없지?"라고 되살리는 것을 막는다) · **정규식 엔진을 `python3` `re`로 고정하는 이유**(`grep -E`는 `(?i)`·`\s`를 조용히 흘린다) · **내부 IP·포트·계정명은 공개 레포에 남긴다는 기존 합의**(이 스펙이 새로 만든 위험이 아니라 명문화). 번호 0018~0021은 hardware-ops 예약, 0022 사용 중 → **0023**. **선행: T5-18**
- [x] **T5-22** (S) 문서 정합 — ① `AGENTS.md:53`·`docs/testing.md`에 `bash scripts/verify-all.sh`(build 제외 기본)를 PR 전 표준으로 추가 ② `docs/branching.md:192`의 "CI(`npm run verify`)"를 **실제 status check 이름 3개**로 정정 ③ 실재하지 않는 게이트를 참조하는 문장 **8곳 전수**에 표기를 단다(§7 드리프트 해소, 실측 8건) — **미구현**: `specs/error-tracking/spec.md:244`·`:425`·`tasks.md:72`·`README.md:124`(`check-error-tracking.sh`) · `specs/observability-alerting/sentry.md:513`(`check-sentry-egress.sh`). **이관**(축3 T3-5가 `scripts/gates/check-runbooks.sh`로 실제 구현하므로 "미구현"이 아니다): `specs/hardware-ops/tasks.md:59`·`spec.md:645` · `specs/observability-alerting/sentry.md:472`. ⚠️ **hardware-ops 2곳은 T3-8이 이미 담당** — T5-22는 중복 편집하지 않고 표기 유무만 확인한다(AC-5-19가 양쪽을 함께 판정). ④ `specs/sre-addons/backlog.md:38`에 molecule 기각 상태 기록. **선행: T5-18.** 검증: AC-5-19

### 5.D 도입 (사람)

- [ ] **T5-23** `[server]` (S) PR 1건을 열어 **3개 잡이 전부 green인지 확인**하고 실제 소요 시간을 기록. 실패 시 원인을 게이트별로 분리해 T5-1~T5-17로 되돌린다. **선행: T5-18.** 검증: AC-5-15
- [ ] **T5-24** `[server]` (S) **1주 관찰 기간** — required로 등록하지 않은 채 정보성으로 돌려 오탐·플레이키(특히 docker pull)를 수집한다. **이 기간 없이 required로 올리면 red가 일상이 되어 게이트가 무시된다**(지금 check:secrets에 일어난 일). **선행: T5-23**
- [ ] **T5-25** `[server]` (S) GitHub Settings → Branches → main·dev 보호에 required status checks `console`·`repo-gates`·`infra-iac` 등록 + "Require branches to be up to date" + "Do not allow bypassing". **소유자 계정에서만 가능**(이 환경 gh는 협업자 토큰). `docs/branching.md` §7① 체크리스트에 체크. **선행: T5-24.** 검증: AC-5-18

---

## 백로그 (근거 부족 또는 발견 선행 — 이 스펙에서 하지 않는다)

- [ ] **FB-01** `smartctl_exporter` 업스트림 기여 — B1(`buildDeviceLabel` cciss 인덱스) · B2(`interface_ == "scsi"` 게이트) 패치 제안. 축2 채택 여부와 무관하게 커뮤니티 가치가 있다
- [ ] **FB-02** RAID 어레이 상태(degraded/rebuilding/스페어) — `ssacli`/`hpssacli` 부재. HPE MCP 저장소 vendoring 검토 후 판단
- [ ] **FB-03** data05 물리 디스크 대수 확정 — T2-15가 실측한다(hardware-ops T0-6 해소 후, 또는 그전이면 대화형 `sudo`로 — README §4.2.1). **실측 전까지 spec에 추정치를 적지 않는다**
- [ ] **FB-04** data01 hpsa `-d cciss,N` 지원 여부 — T2-17이 판정. 미지원이면 27.3T LV이 사각지대로 남는 사실을 spec에 명시
- [ ] **FB-05** 랙 전력 예산·PSU 정격 실사 → 전력 알림의 조치 가능성 재평가(현재는 조치 불명확으로 패널만)
- [ ] **FB-06** molecule 도입 재검토 — 타깃 균질화(xenial 정리)가 선행되면 비용 구조가 바뀐다. ADR-0023 기각 근거 참조
- [ ] **FB-07** `check-error-tracking.sh`·`check-sentry-egress.sh` 구현 — 각각 error-tracking·sentry 축 소관. `scripts/gates/`에 떨어지면 `verify-all.sh`가 자동 편입한다(배선 작업 0)
